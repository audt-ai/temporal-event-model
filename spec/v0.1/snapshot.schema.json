{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Audt/temporal-event-model/spec/v0.1/snapshot.schema.json",
  "title": "Snapshot",
  "description": "Complete state of an entity at a specific point in time",
  "type": "object",
  "required": ["entity_id", "as_of", "state"],
  "properties": {
    "entity_id": {
      "type": "string",
      "description": "Identifier for the entity"
    },
    "as_of": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp indicating when this snapshot is valid"
    },
    "state": {
      "type": "object",
      "description": "Complete state of the entity. Structure varies by entity type."
    },
    "last_event_id": {
      "type": "string",
      "description": "Identifier of the most recent event that contributed to this snapshot"
    },
    "deleted": {
      "type": "boolean",
      "description": "True if the entity has been deleted as of this snapshot",
      "default": false
    }
  },
  "additionalProperties": false
}

